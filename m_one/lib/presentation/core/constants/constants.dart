import 'package:flutter/material.dart';

const blueColor = Color(0xFF1FA5C3);

// Light Theme
const primaryColorLight = Color(0xFF1A5CFF);

const headerBackgroundColorLightTheme = Color(0xFFFFFFFF);
const headerForegroundColorLightTheme = Color(0xFF000000);
const secondaryColorLightTheme = Color(0xFFE9780B);
const surfaceColorLightTheme = Color(0xFFBFDBFE);
const textColorLightTheme = Color(0xFF132144);
const bodyTextColorLightTheme = Color(0xFF788198);

// Dark Theme
const primaryColorDark = Color(0xFF1A5CFF);

const headerBackgroundColorDarkTheme = Color(0xFF29283C);
const headerForegroundColorDarkTheme = Color(0xFFFFFFFF);
const secondaryColorDarkTheme = Color(0xFFE9780B);
const bgColorDarkTheme = Color(0xFF1E1E1E);
const surfaceColorDarkTheme = Color(0xFF1E293B);
const textColorDarkTheme = Colors.white;
const bodyTextColorDarkTheme = Color(0xFF9CA5BA);

const defaultPadding = 15.0;
const defaultBorderRadius = 30.0;

final defaultShadowColor = Colors.grey.withOpacity(0.01);
const defaultShadowOffset = Offset(0, 10);
const defaultShadowSpreadRadius = 1.0;
const defaultShadowBlurRadius = 100.0;
